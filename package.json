{
  "name": "wp-podnest",
  "version": "1.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build":       "node build.mjs",
    "build:watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}